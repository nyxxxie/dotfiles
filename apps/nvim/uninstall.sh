#!/bin/bash
# TODO: use the NVIM_CONFIG, etc env variables in install
# TODO: coc.nvim installs stuff with npm, uninstall it.
rm -rf "$HOME/bin/nvim"*
rm -rf "$HOME/.config/nvim/" "$HOME/.local/share/nvim/"
